let data = [];
let currectSize = data.length;
let max = 5;
function push(newval){
  if(currentSize = max){
    alert("stack is full you can not add"+newval);
  }
  data[currentSize] = newval;
  currentSize+=1;
}

push(20);
push(30);
push(20);
push(2);
push(25);
push(23);
push(67);

function pop(){
  if(currentSize > 0){
    if(currentSize > 1){
      currentSize -=1;
      data.lenght= currentSize;
    }else{
      alert("stack is alredy empty");
    }
  }
}
